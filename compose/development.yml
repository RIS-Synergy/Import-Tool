services:
  backend:
    volumes:
      - ../backend/:/app
    environment:
      - NODE_ENV=development
      - RIS_USE_DEV=2024-06-19_projects.json

  frontend:
    volumes:
      - ../frontend/:/app
    ports:
      # - "3032:3000"
      - "24678:24678"
