services:
  # db:
  #   image: postgres:16.3-alpine3.20
  #   container_name: ris-db
  #   ports:
  #     - 3030:5432

  #   shm_size: 128mb # set shared memory limit when using docker-compose

  #   volumes:
  #     - ris_db:/var/lib/postgresql/data

  #   environment:
  #     POSTGRES_USER: ris-user
  #     POSTGRES_PASSWORD_FILE: /run/secrets/db_password
  #     POSTGRES_DB: ris_db
  #   secrets:
  #     - db_password

  # backend:
    # build:
    #   context: ./backend
    # container_name: ris-backend
    # depends_on:
    #   - db
    # secrets:
    #   - db_password
    #   - fa_api_client_secret
    #   - ri_api_key
    # environment:
    #   - NODE_ENV=production
    # ports:
    #   - "3031:3001"

  # frontend:
  #   build:
  #     context: ./frontend
  #   container_name: ris-frontend
  #   volumes:
  #     - ./frontend/:/app
  #   ports:
  #     - "3032:3000"
  #     - "24678:24678"

# secrets:
#    db_password:
#      variable: dummy

# volumes:
#     ris_db:
